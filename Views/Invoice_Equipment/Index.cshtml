@model IEnumerable<RentalManagement.Models.Invoice_Equipment>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Equipment.Equipment_Category)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Invoice.Invoice_ID)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Equipment.Equipment_Category)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Invoice.Invoice_ID)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.InvoiceEquipment_ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.InvoiceEquipment_ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.InvoiceEquipment_ID })
        </td>
    </tr>
}

</table>
